export function selectBook(book) {
  //selectBook is an action creator that needs to return an action (object)
  return {
    type:'BOOK_SELECTED',
    payload:book
  }
}


export function increment() {
  //selectBook is an action creator that needs to return an action (object)
  return {

    type:'INCREMENT',
    
  }
}


export function decerment() {
  //selectBook is an action creator that needs to return an action (object)
  return {
    type:'DECERMENT',
    
  }
}

export function clear() {
  //selectBook is an action creator that needs to return an action (object)
  return {
    type:'CLEAR',
    
  }
}

export function  add() {
  //selectBook is an action creator that needs to return an action (object)
  return {
    type:'ADD',
    
  }
}

export function  sub() {
  //selectBook is an action creator that needs to return an action (object)
  return {
    type:'SUB',
    
  }
}




